---
sidebar_position: 2
---

# Initialize a Local Git Repo

我們可以透過終端機指令初始化 Git repository:

```bash
$ git init
```

Git 以前預設 branch 名稱叫 `master`。不過現在一般改叫 `main` 了。
以下的範例也都預設叫 `main`。如果發現自己的還叫 `master`，可以用 `git branch -M main` 改。

這時你的 git graph 像這樣：

```mermaid
gitGraph
```
